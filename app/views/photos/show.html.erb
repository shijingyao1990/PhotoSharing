<!DOCTYPE html>
<html>
<head>
	<title>Individual Photo</title>
	
	
	
</head>
<body>
	<div class ="Photo"><%= image_tag(@photo.file_name) %></div>
	<% @comments.each do |comment| %>

			<div class = "comment_time"><p><%= comment.created_at %></p></div>
			<div class = "comment_name"><p>written by <%= "#{comment.user.first_name}, #{comment.user.last_name}" %></p></div>
			<div class = "comment_content"><%= comment.comment %></div>
	<% end %>
</body>
</html>